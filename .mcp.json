{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "Local repository filesystem access"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "description": "GitHub issues/PRs (requires GITHUB_PERSONAL_ACCESS_TOKEN)",
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent key-value memory for agent sessions"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "description": "HTTP fetch for public docs"
    },
    "git": {
      "command": "npx",
      "args": ["-y", "@cyanheads/git-mcp-server"],
      "description": "Optional git MCP (install may vary; use gh/git CLI if unavailable)"
    },
    "codebase-memory-mcp": {
      "command": "codebase-memory-mcp",
      "args": [],
      "description": "DeusData knowledge-graph indexes (.codebase-memory/graph.db.zst). Install: pip install codebase-memory-mcp"
    }
  },
  "_notes": {
    "codebase-memory": "Primary: codebase-memory-mcp (graph.db.zst). Lightweight: just memory (ctags/lists). See docs/engineering/codebase-memory.md and .cursor/mcp.json.example.",
    "sequential-thinking": "Enable in host agent if available; not vendored here.",
    "security": "Do not commit real tokens. Map secrets via environment only."
  }
}
