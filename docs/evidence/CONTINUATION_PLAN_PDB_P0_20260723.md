# Continuation Plan: Product Database P0 (Issue #68)

**Date:** 2026-07-23  
**Source SHA:** 3d86ee1e5c20909f005c3688f70afd0f66256aa3  
**Context:** Post-inventory receipt (CATALOG_INVENTORY_RECEIPT_20260723.md). Static PDB-01 complete. Models stub started.  
**Goal:** Advance canonical Device Registry + public Product Database foundation to a verifiable, usable state within current constraints (no full runtime DB in this session).

## Scope (from PRODUCT_DATABASE_COMPLETENESS_ROADMAP + DEVICE_REGISTRY + ADR-006 + Issue #68)
- Complete models for full hierarchy.
- Ingest existing high-quality seeds (synth-catalog Phase 2).
- Produce deterministic snapshot + coverage receipt.
- Basic query services.
- API surface stubs.
- Preserve compatibility with ModuleCatalog / existing consumers.
- No audio/DSP/hardware. Fail-closed on unknowns.

## Phased Execution Plan (this session)

### Phase 1 — Models & Structure (PDB-02)
- [x] Inventory receipt (prior).
- [ ] Full SQLAlchemy models for:
  - Manufacturer
  - DeviceFamily
  - DeviceModel
  - DeviceRevision
  - Port, Control, Capability (as first-class or rich on revision)
  - Basic Evidence / Alias support
- [ ] Registry base classes + snapshot serialization helpers.

**Exit:** `backend/registry/models.py` ruff-clean, matches `backend/modules/models.py` style, full hierarchy present.

### Phase 2 — Ingestion & Snapshot (PDB-01 extension)
- [ ] Script `scripts/ingest_registry_from_seeds.py` that:
  - Loads `data/synth-catalog/seed-phase2-v1.json` (and similar).
  - Maps to registry entities.
  - Generates `data/registry_snapshots/registry_YYYYMMDD.json` + hash.
  - Computes coverage stats (brands, modules with HP, etc.).
- [ ] Pure-Python fallback execution (no DB required).

**Exit:** Snapshot file + new receipt with exact counts and provenance.

### Phase 3 — Services & Queries
- [ ] `backend/registry/services.py`:
  - list_manufacturers()
  - get_manufacturer(slug)
  - search_models(q, brand=None, category=None)
  - get_coverage_report()
  - build_snapshot()

**Exit:** Usable in-memory / file-backed queries. Can be wired to DB later.

### Phase 4 — API Surface (PDB-03)
- [ ] `backend/registry/routes.py` (FastAPI router stubs):
  - GET /registry/manufacturers
  - GET /registry/manufacturers/{slug}
  - GET /registry/search
  - GET /registry/coverage
- [ ] Minimal Pydantic response schemas.
- [ ] Note for mounting in main.py (non-breaking).

**Exit:** Routes file that would import cleanly.

### Phase 5 — Evidence & State
- [ ] New receipt: `docs/evidence/PDB_P0_PHASE1_REGISTRY_FOUNDATION.md`
- [ ] Update `CURRENT_STATE.md` and inventory receipt.
- [ ] Update todo / plan status.

**Exit:** Clear "what was delivered" + next operator steps.

## Constraints & Safety
- No full `alembic` run or live DB in this execution (env blocker).
- Use static + file-based snapshot for verifiable output.
- All unknown fields remain null/unknown (never invent).
- Keep dual-path compatibility (do not break ModuleCatalog).
- Ruff + syntax clean required before considering slice done.
- No production claims.

## Verification Commands (to run at each phase)
```bash
ruff check backend/registry/ scripts/
python -m py_compile backend/registry/*.py scripts/ingest*.py
python -c "from backend.registry import models; print('models import ok (structure)')"
# Later: full pytest after env setup
```

## Exit Criteria for this Plan Session
- Models complete + clean.
- At least one seed ingested to snapshot with measurable coverage.
- Services + route stubs exist.
- Fresh receipts + state docs updated.
- All runnable verification (ruff/syntax/static) green.
- Clear handoff for next (DB wiring, explorer UI, admin).

**Status at plan creation:** Phase 1 partial (stub existed). Executing to completion now.

**Generated by:** Hermes Agent following repo continuation discipline.
